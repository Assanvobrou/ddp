// Utilitaire twemoji — stub pour compatibilité
// Le projet utilise les emojis natifs du navigateur (unicode)
// Twemoji peut être activé en important la lib et en parsant le document
export const twemoji_parse = (text: string) => text
